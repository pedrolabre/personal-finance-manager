<!-- PendenciasListView.xaml -->
<UserControl x:Class="PersonalFinanceManager.Views.Pendencias.PendenciasListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:enums="clr-namespace:PersonalFinanceManager.Models.Enums"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="950">
    
    <UserControl.Resources>
        <!-- Estilo de linha para pendências quitadas -->
        <Style x:Key="QuitadaRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static enums:StatusPendencia.Quitada}">
                    <Setter Property="Background" Value="#E8F5E9"/>
                    <Setter Property="Foreground" Value="#2E7D32"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="{x:Static enums:StatusPendencia.Atrasada}">
                    <Setter Property="Background" Value="#FFEBEE"/>
                    <Setter Property="Foreground" Value="#C62828"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Cabeçalho -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Pendências" 
                      Style="{StaticResource TitleTextBlockStyle}"
                      VerticalAlignment="Center"/>
            
            <Button Content="➕ Nova Pendência"
                   Command="{Binding NovaPendenciaCommand}"
                   Style="{StaticResource PrimaryButtonStyle}"                   Height="40"
                   MinWidth="150"                   HorizontalAlignment="Right"/>
        </Grid>
        
        <!-- Barra de Filtro e Ações -->
        <Border Grid.Row="1" 
               Style="{StaticResource CardStyle}"
               Margin="0,0,0,20"
               Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox Grid.Column="0"
                        Text="{Binding FiltroTexto, UpdateSourceTrigger=PropertyChanged}"
                        Height="35"
                        Padding="10,0"
                        VerticalContentAlignment="Center"
                        BorderBrush="{StaticResource BorderColor}"
                        BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                
                <TextBlock Grid.Column="0"
                          Text="🔍 Buscar pendências..."
                          IsHitTestVisible="False"
                          VerticalAlignment="Center"
                          Margin="15,0,0,0"
                          Foreground="{StaticResource TextSecondaryColor}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FiltroTexto}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                
                <Button Grid.Column="1"
                       Content="🔄 Atualizar"
                       Command="{Binding AtualizarCommand}"
                       Style="{StaticResource SecondaryButtonStyle}"
                       Margin="10,0,0,0"/>
            </Grid>
        </Border>
        
        <!-- Lista de Pendências -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <DataGrid ItemsSource="{Binding PendenciasFiltradas}"
                     AutoGenerateColumns="False"
                     IsReadOnly="True"
                     SelectionMode="Single"
                     HeadersVisibility="Column"
                     GridLinesVisibility="None"
                     Background="Transparent"
                     BorderThickness="0"
                     RowHeight="60"
                     RowStyle="{StaticResource QuitadaRowStyle}">
                
                <DataGrid.Columns>
                    <!-- Nome e Descrição -->
                    <DataGridTemplateColumn Header="Pendência" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="10,0">
                                    <TextBlock Text="{Binding Nome}"
                                              FontWeight="SemiBold"
                                              FontSize="14"/>
                                    <TextBlock Text="{Binding Descricao}"
                                              FontSize="12"
                                              Foreground="{StaticResource TextSecondaryColor}"
                                              Margin="0,2,0,0"
                                              TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Valor Total -->
                    <DataGridTextColumn Header="Valor Total" 
                                       Binding="{Binding ValorTotal, StringFormat=C}"
                                       Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Valor Pago -->
                    <DataGridTextColumn Header="Pago" 
                                       Binding="{Binding ValorPago, StringFormat=C}"
                                       Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,10,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    
                    <!-- Progresso -->
                    <DataGridTemplateColumn Header="Progresso" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="10,0">
                                    <ProgressBar Value="{Binding PercentualPago, Mode=OneWay}"
                                               Maximum="100"
                                               Height="8"
                                               Foreground="{StaticResource SuccessColor}"/>
                                    <TextBlock Text="{Binding PercentualPago, Mode=OneWay, StringFormat={}{0:F0}%}"
                                              FontSize="10"
                                              HorizontalAlignment="Center"
                                              Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Prioridade -->
                    <DataGridTemplateColumn Header="Prioridade" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Prioridade, Converter={StaticResource PrioridadeToColorConverter}}"
                                       CornerRadius="3"
                                       Padding="8,4"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Prioridade}"
                                              Foreground="White"
                                              FontSize="11"
                                              FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Status -->
                    <DataGridTemplateColumn Header="Status" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                                       CornerRadius="3"
                                       Padding="8,4"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Status}"
                                              Foreground="White"
                                              FontSize="11"
                                              FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Ações -->
                    <DataGridTemplateColumn Header="Ações" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" 
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center">
                                    <Button Content="👁️"
                                           Command="{Binding DataContext.VisualizarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding Id}"
                                           ToolTip="Visualizar"
                                           Width="26"
                                           Height="26"
                                           Margin="1"
                                           FontSize="11"
                                           Style="{StaticResource SecondaryButtonStyle}"/>
                                    
                                    <Button Content="✏️"
                                           Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding Id}"
                                           ToolTip="Editar"
                                           Width="26"
                                           Height="26"
                                           Margin="1"
                                           FontSize="11"
                                           Style="{StaticResource SecondaryButtonStyle}"/>
                                    
                                    <Button Content="✓"
                                           Command="{Binding DataContext.QuitarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding Id}"
                                           ToolTip="Quitar"
                                           Width="26"
                                           Height="26"
                                           Margin="1"
                                           FontSize="11"
                                           Style="{StaticResource SuccessButtonStyle}"/>
                                    
                                    <Button Content="🗑️"
                                           Command="{Binding DataContext.ExcluirCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding Id}"
                                           ToolTip="Excluir"
                                           Width="26"
                                           Height="26"
                                           Margin="1"
                                           FontSize="11"
                                           Style="{StaticResource DangerButtonStyle}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        
        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3"
               Background="#80000000"
               Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Carregando..."
                      Foreground="White"
                      FontSize="20"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>

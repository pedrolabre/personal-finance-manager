<!-- MainWindow.xaml: Main application shell -->
<Window x:Class="PersonalFinanceManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Personal Finance Manager" 
    Height="700" 
    Width="1200"
    WindowStartupLocation="CenterScreen"
    Background="{StaticResource BackgroundColor}">
    
    <Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="250"/>
        <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
        
    <!-- Menu Lateral -->
    <Border Grid.Column="0" 
        Background="{StaticResource SidebarColor}"
        BorderBrush="{StaticResource BorderColor}"
        BorderThickness="0,0,1,0">
            
        <StackPanel Margin="0,20,0,0">
        <!-- Logo/Título -->
        <TextBlock Text="💰 Finance Manager"
              FontSize="20"
              FontWeight="Bold"
              Foreground="White"
              HorizontalAlignment="Center"
              Margin="0,0,0,30"/>
                
        <!-- Menu Principal -->
        <Button Content="📊 Dashboard"
            Command="{Binding NavigateToDashboardCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Button Content="📋 Pendências"
            Command="{Binding NavigateToPendenciasCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Button Content="💳 Cartões"
            Command="{Binding NavigateToCartoesCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Button Content="🤝 Acordos"
            Command="{Binding NavigateToAcordosCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Button Content="💵 Recebimentos"
            Command="{Binding NavigateToRecebimentosCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Separator Margin="10,20" Background="#FFFFFF33"/>
                
        <!-- Menu Secundário -->
        <Button Content="📥 Importar"
            Command="{Binding NavigateToImportCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Button Content="📄 Relatórios"
            Command="{Binding NavigateToReportsCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
                
        <Separator Margin="10,20" Background="#FFFFFF33"/>
                
        <Button Content="⚙️ Configurações"
            Command="{Binding NavigateToSettingsCommand}"
            Style="{StaticResource MenuButtonStyle}"/>
        </StackPanel>
    </Border>
        
    <!-- Área de Conteúdo -->
    <Grid Grid.Column="1">
        <ContentControl Content="{Binding CurrentView}" Margin="0" />
    </Grid>
    </Grid>
</Window>
